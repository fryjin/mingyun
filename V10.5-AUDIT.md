# V10.5 架构收口审计

## GitHub 基线

本轮以 `fryjin/mingyun` 当前 `main` 的 V10.4 提交 `8a802fd2181630d9191a536e6f7cda23ba20d29f` 为唯一基线。

## 收口发现

V10.4 已让 12 个运行游戏全部进入 V10 插件入口，但仓库仍有以下双轨残留：

- `Application` 仍从 `src/games/shared.js` 获取活动的 `createGameContext`。
- `src/main.js` 是旧应用入口，并继续引用 `games/shared.js`。
- `src/modules/games.js` 是旧注册/运行模块，并直接导入 Legacy 游戏文件。
- 12 个 `src/games/*.js` Legacy 游戏实现仍保留。
- Registry 默认仍保留 legacy source / contract v1 兼容能力。
- 根 `app.js` / `app.css` / `manifest.json` 为旧入口残片。
- 四个当前仍加载的游戏样式继续使用 V9 版本号文件名；命运叠塔另有三份已被替代旧样式。
- `vite.config.js` 的构建 Service Worker 版本仍停留在 10.0.0。
- 根 README / CHANGELOG / MIGRATION / PATCH 报告仍是 V9 历史内容。

## V10.5 处理

- 将活动游戏上下文与惩罚 UI 桥接迁移到 `src/app/game-context.js`，保持业务行为不变。
- `Application` 改为引用新的 app 层上下文。
- 删除旧 `src/main.js`、`src/modules/games.js`、12 个 Legacy 游戏和 `src/games/shared.js`。
- Registry 只接受 contract v2 与 source=v10。
- 删除根旧入口文件与重复 manifest。
- 活跃样式仅更名，不修改 CSS 内容。
- 删除被替代的命运叠塔旧样式。
- 版本统一到 10.5.0，并让 Vite 生产 SW 使用 10.5.0。
- 新增 `check:architecture`、CI、GitHub Pages `dist/` 发布链路。
- 更新最终架构、迁移、部署、回滚和根项目文档。

## 本地验证

- V10.5 architecture validator：PASS。
- Registry 收口 + FrameLoop + AnimationRegistry + 命运叠塔回归：18 / 18 PASS。
- 补丁应用脚本：从模拟 V10.4 基线执行 PASS；执行后 `src/games/` 仅剩 `index.js`、`registry.js`。
- overlay 全部 JS/MJS `node --check`：PASS。
- OPERATIONS JSON：PASS。
- GitHub Actions YAML：PASS。

## 构建边界

当前执行容器无法访问 GitHub/npm 网络，且没有预装 Vite，因此没有声称本地 `npm run build` 已执行。补丁中的 CI/Pages workflow 会在 GitHub runner 安装固定的 `vite@8.1.5` 并完成正式 `verify` 与 `dist/` 构建。

## 玩法锁定

V10.5 不修改任何游戏规则、题库、设置字段或命运叠塔 V10.4 的物理/镜头/反馈参数。
