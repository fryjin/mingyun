# V9.1.1 测试报告

测试日期：2026-08-03

## 自动数据校验

- 共享惩罚题库：3500，ID 与规范化正文唯一。
- 专用内容：2500，五个专用内容游戏各 500。
- 实际启用总内容：6000。
- JSON 数量、字段和 SHA-256 摘要通过。
- 谁是卧底 500 组词库重新生成并完成机械拼接词检查。
- Service Worker 缓存版本为 `party-game-v9.1.1`。

## JavaScript 与 PWA 静态校验

- `src/**/*.js` 全部通过 `node --check`。
- `sw.js` 通过 `node --check`。
- `manifest.webmanifest`、共享题库 Manifest、专用内容 Manifest 均可解析。
- 二选一源码不含随机阵营或纯讨论结算入口。
- 炸弹传递源码不含随机方向和具体持有者跟踪。
- 谁是卧底私密页不含身份展示文案。
- 国王游戏使用单一步骤生成任务和执行号码。

## Chromium 移动端回归

实际视口：

- 390 × 844
- 360 × 800

通过项目：

- 大厅正常展示 8 种游戏。
- 二选一只展示少数派／多数派。
- 国王游戏 6 名玩家逐人查看号码后，通过一个按钮同时抽取任务和执行号码。
- 谁是卧底 5 个发言时限在 390px 和 360px 下均保持单行。
- 谁是卧底私密页只显示词语，不显示身份。
- 炸弹传递只显示顺时针／逆时针，运行中不显示玩家姓名，也没有屏幕传递按钮。
- 骰子使用六面 3D DOM 结构，投掷期间存在连续三维旋转。
- 转盘 RAF 旋转正常，指针轻动画不会破坏原有居中变换。
- 360px 页面无横向溢出。
- 浏览器运行时错误：0。

详细机器结果：

```text
docs/v9.1.1-tests/browser-report.json
```

## Anime.js 接入检查

- 已验证 `src/core/motion.js` 能识别 Anime.js V4 WAAPI 模块导出的 `waapi.animate()`。
- 使用拦截模块模拟官方 WAAPI API，模块加载成功且骰子动效调用次数大于 0。
- 本地测试域名下默认使用原生 WAAPI，避免测试和离线场景等待外部网络。
- 正式在线部署时会预热固定版本的 Anime.js WAAPI 模块；模块未就绪或加载失败时立即回退到原生 WAAPI。
- 转盘主体有连续角速度控制需求，因此仍使用 RAF；Anime.js／WAAPI 只处理指针、容器和结果微动效。

## 人工现场验收建议

自动检查已经覆盖本次五项修改和关键移动端布局。部署后仍建议用真实手机完成：

1. 连续投掷 6 次骰子，确认手感与音效节奏。
2. 分别使用 2、6、12 人转盘，确认标签可读性。
3. 实际传递一轮炸弹，确认隐藏计时带来的节奏是否合适。
4. 完整进行一局谁是卧底，抽检词组难度。
