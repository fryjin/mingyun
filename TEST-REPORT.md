# V9.1.4 测试报告

测试日期：2026-08-03

## 已执行

- 升级包内全部 JavaScript 文件及 `sw.js` 通过 `node --check`。
- `index.html` 可被 HTML 解析器正常解析，并已加载 `adult-plus.css` 与 `ui-refresh.css`。
- `adult-plus.css` 与 `ui-refresh.css` 花括号结构检查通过。
- V9.1.4 差量校验脚本通过。
- 本版新增专用内容共 2000 条：谁最可能、二选一、五秒挑战、谁是卧底各 500 条／组。
- 升级包累计包含成人进阶内容 4000 条：共享真心话／大冒险 1000、五类专用内容 3000。
- 成人进阶题库 ID 与规范化正文唯一；Manifest 数量、字节数和 SHA-256 摘要一致。
- 五秒挑战、国王指令和成人进阶大冒险中，凡涉及接触、亲吻或酒水的任务均包含 `requirements`、逐题同意和替代方案。
- 亲吻内容只使用独立开关，`contactLevel` 均限制在 0–2；未出现设置层级与过滤逻辑冲突。
- 谁是卧底成人进阶 500 组词均为 1–4 字，且不含句式标点。
- 关闭全部身体接触、亲吻和酒水开关时，每个成人进阶题库仍保留可用内容。
- 开启全部边界开关时，升级包内 1000 条共享成人进阶内容和 3000 条成人进阶专用内容均可进入随机池。
- 未发现强迫饮酒、整杯罚饮、脱衣、私密部位接触、限制呼吸、捆绑、疼痛动作、偷拍或强制公开内容。
- 炸弹传递时长代码与显示文案一致：10–20 / 20–40 / 40–60 秒。
- Service Worker 缓存版本为 `party-game-v9.1.4`，并包含新增 UI 样式文件。

## UI 静态检查

- 主要触控控件最小高度统一到 44px。
- 五档内容尺度在 360px 下使用两列布局，“成人进阶”独占整行。
- 游戏详情抽屉使用独立滚动区和固定底部主按钮。
- 成人与成人进阶确认在原设置抽屉内完成，取消后不会丢失已选配置。
- 弹层包含显式关闭按钮、Esc 处理、Tab 焦点锁定和关闭后焦点恢复。
- 玩家暂离状态同时使用文字、状态点和透明度，不只依赖颜色。
- 游戏进行页、私密传递页、投票按钮和结果按钮均提高了触控面积和文本间距。

## 未执行

当前环境没有完整 V9.1.3 仓库副本，因此未执行覆盖后的真实 GitHub Pages 部署测试和手机浏览器自动化截图测试。

部署后重点人工验证：

1. 360px、390px、430px 屏幕下游戏详情抽屉的滚动与固定按钮。
2. 五种尺度的选中状态、成人进阶面板展开和取消确认后的状态保留。
3. 谁最可能、二选一、五秒挑战、谁是卧底的成人进阶题库加载。
4. 五秒挑战逐题确认、替代挑战和换题。
5. 炸弹传递三档实际爆炸时间范围。
6. Service Worker 从旧版本升级后的缓存刷新。
