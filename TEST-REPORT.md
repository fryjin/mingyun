# V9.1.2 测试报告

测试日期：2026-08-03

## 自动数据校验

- 共享惩罚题库：3500。
- 专用内容：2500，五个专用内容游戏各 500。
- 实际启用总内容：6000。
- JSON 数量、字段、ID、规范化内容和 SHA-256 摘要通过。
- 二选一 500 条内容均包含情境问题、A 选项和 B 选项，且 A/B 不相同。
- 谁是卧底 500 组词语均为 1–4 字，无句号、逗号、问号等句子标点。
- Service Worker 缓存版本为 `party-game-v9.1.2`。

## JavaScript 静态校验

- `src/**/*.js` 全部通过 `node --check`。
- `sw.js` 通过 `node --check`。
- `manifest.webmanifest`、共享题库 Manifest、专用内容 Manifest 均可解析。
- `game-sheet.js` 使用原子 `startGame()`，不再先持久化设置、再单独切换路由。
- 转盘源码包含木圈、金属环、白球轨道、轮盘主体和中轴五层结构。
- 谁是卧底私密页不存在身份展示字段。

## Chromium 移动端回归

实际视口：390 × 844，并追加 360 × 800 窄屏检查。

浏览器使用项目真实 ES Module，通过测试路由拦截加载本地文件；运行时未修改业务模块。

通过项目：

- 大厅展示 8 种游戏。
- 二选一点击开始后直接出现游戏情境，期间未重新出现大厅卡片。
- 二选一实测情境：“群聊里出现尴尬冷场，你会怎么处理？”
- 对应选项：“主动找新话题”与“安静等别人说”。
- 赌场转盘 6 个玩家槽位正常生成。
- 木质外圈、金属环、白球轨道和中轴均存在。
- 转动后轮盘与白球的 CSS 变换均发生变化。
- 谁是卧底实测词语长度不超过 4 字。
- 谁是卧底私密区域未出现“你的身份”“平民”或“卧底”身份提示。
- 360px 大厅和转盘均无横向溢出，转盘显示宽度为 300px。
- 浏览器控制台错误：0。
- 页面运行时异常：0。

机器结果：

```text
docs/v9.1.2-tests/browser-report.json
```

截图：

```text
docs/v9.1.2-tests/would-rather-live.png
docs/v9.1.2-tests/casino-wheel-live.png
docs/v9.1.2-tests/casino-wheel-live-motion.png
docs/v9.1.2-tests/undercover-short-live.png
```

## 人工现场验收建议

1. 连续进入不同游戏，确认设置抽屉关闭后没有大厅闪屏。
2. 抽检四档二选一内容，确认 A/B 都能形成真实选择冲突。
3. 使用 2、6、12 人转盘检查姓名密度和白球停止位置。
4. 完整进行一局谁是卧底，抽检词组是否既相近又可区分。
