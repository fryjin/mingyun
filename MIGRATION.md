# 部署说明

## 前置版本

必须先部署并确认使用：

```text
V9.2.2 Logic Lock
```

本补丁不包含任何游戏 JavaScript，因此不会自行恢复或替换游戏逻辑。

## 覆盖文件

只覆盖以下三个文件：

```text
index.html
sw.js
styles/midnight-game-hall.css
```

不要上传 `README.md` 等说明文件到线上根目录也可以，不影响运行。

## 部署后

1. 等待 Pages 部署完成。
2. 关闭当前浏览器标签页。
3. 重新打开站点。
4. 出现“新版本已准备好”时点击“立即更新”。
5. 若仍显示旧样式，清除该站点缓存后重新进入。
