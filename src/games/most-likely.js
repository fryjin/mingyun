import { registerGame } from './registry.js';
import { adultPlusFilterKey, allowedByAdultPlus, drawGame } from '../modules/questions.js';
import { escapeHtml, randomInt } from '../core/utils.js';
import { bindExit, passScreen, stageHeader } from './shared.js';
const plugin={id:'most-likely',title:'谁最可能',sortOrder:6,icon:'vote',color:'#22d3ee',minPlayers:3,maxPlayers:12,supportsAdult:true,estimatedTime:'1–2 分钟',shortDescription:'全员秘密投票，最后统一揭晓结果。',description:'所有玩家依次私密投票，投票过程中不显示任何票数。全部完成后统一揭晓得票最高者。',phoneMode:'逐人私密传递',resultMode:'最高票玩家接受惩罚',defaultSettings:{tieRule:'all',level:'standard'},renderSetup(settings){return `<div class="setting-block"><div class="setting-label"><span>平票处理</span><small>最高票并列时</small></div><div class="segmented" data-tie><button type="button" data-segment data-value="all" class="${settings.tieRule==='all'?'active':''}">并列全部</button><button type="button" data-segment data-value="random" class="${settings.tieRule==='random'?'active':''}">随机一人</button></div></div>`},readSetup(sheet){return {tieRule:sheet.querySelector('[data-tie] .active')?.dataset.value||'all'}},async mount(root,ctx){let prompt,voterIndex=0,votes=[];try{const prefs=ctx.settings.adultPlus||{};prompt=await drawGame('most-likely',ctx.settings.level,item=>ctx.settings.level!=='adult-plus'||allowedByAdultPlus(item,prefs),ctx.settings.level==='adult-plus'?adultPlusFilterKey(prefs):'all')}catch(e){root.innerHTML=`${stageHeader(plugin.title)}<section class="game-stage centered"><p>${escapeHtml(e.message)}</p></section>`;bindExit(root,ctx);return}const intro=()=>{root.innerHTML=`${stageHeader(plugin.title,'无主持人 · 隐藏投票')}<section class="game-stage centered"><span class="eyebrow">本轮问题${ctx.settings.level==='adult-plus'?' · 成人进阶':''}</span><p class="feature-question">${escapeHtml(prompt.text)}</p><p>所有人看清题目后，开始逐人秘密投票。</p><button class="button primary full" data-begin>开始秘密投票</button></section>`;bindExit(root,ctx);root.querySelector('[data-begin]').onclick=pass};const pass=()=>{const voter=ctx.players[voterIndex];root.innerHTML=`${stageHeader(plugin.title)}${passScreen(voter,'进入投票')}`;bindExit(root,ctx);root.querySelector('[data-private-open]').onclick=()=>vote(voter)};const vote=voter=>{root.innerHTML=`${stageHeader(plugin.title)}<section class="private-stage"><span class="eyebrow">${escapeHtml(voter.name)} 的秘密投票</span><h2>谁最符合？</h2><p class="private-question">${escapeHtml(prompt.text)}</p><div class="player-choice-grid">${ctx.players.map(p=>`<button data-candidate="${p.id}">${escapeHtml(p.name)}</button>`).join('')}</div><button class="button ghost full" data-skip>跳过本题</button></section>`;bindExit(root,ctx);root.querySelectorAll('[data-candidate]').forEach(b=>b.onclick=()=>save(b.dataset.candidate));root.querySelector('[data-skip]').onclick=()=>save(null)};const save=candidate=>{if(candidate)votes.push(candidate);voterIndex++;voterIndex>=ctx.players.length?ready():pass()};const ready=()=>{root.innerHTML=`${stageHeader(plugin.title)}<section class="game-stage centered"><span class="eyebrow">投票完成</span><h2>所有选择均已锁定</h2><p>点击后将公开票数和玩家名单。</p><button class="button primary full" data-reveal>揭晓结果</button></section>`;bindExit(root,ctx);root.querySelector('[data-reveal]').onclick=reveal};const reveal=()=>{const counts=new Map(ctx.players.map(p=>[p.id,0]));votes.forEach(id=>counts.set(id,(counts.get(id)||0)+1));const max=Math.max(...counts.values()),winners=ctx.players.filter(p=>counts.get(p.id)===max);const losers=ctx.settings.tieRule==='random'&&winners.length>1?[winners[randomInt(0,winners.length-1)]]:winners;root.innerHTML=`${stageHeader(plugin.title)}<section class="game-stage"><span class="eyebrow">揭晓结果</span><h2>${escapeHtml(losers.map(p=>p.name).join('、'))} 遭殃</h2><div class="vote-results">${[...ctx.players].sort((a,b)=>counts.get(b.id)-counts.get(a.id)).map(p=>`<div><span>${escapeHtml(p.name)}</span><span class="vote-bar"><i style="width:${max?counts.get(p.id)/max*100:0}%"></i></span><strong>${counts.get(p.id)} 票</strong></div>`).join('')}</div><button class="button primary full" data-punish>抽取惩罚</button><button class="button secondary full" data-next>换一道题</button></section>`;bindExit(root,ctx);root.querySelector('[data-punish]').onclick=()=>ctx.punishment(losers,{onDone:()=>plugin.mount(root,ctx)});root.querySelector('[data-next]').onclick=()=>plugin.mount(root,ctx)};intro()}};registerGame(plugin);export default plugin;
